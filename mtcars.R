# comment to eplain ur code
mtcars
?mtcars
?mean
class(mtcars)
summary(mtcars) #summary of data
?summary
str(mtcars)
?str
table(mtcars$gear) #to know the number of gear types
table(mtcars$cyl)
table(mtcars$cyl, mtcars$gear, dnn= c("cyl","gear"))
